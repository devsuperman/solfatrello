@page "/tareas/form/{Id:int?}"
@attribute [Authorize]

<PageTitle>
    @Titulo
</PageTitle>

<h1>
    <i class="bi bi bi-coin"></i>
    @Titulo
</h1>

<hr />

<EditForm method="post" Model="Model" OnValidSubmit="Save" FormName="form" Enhance>

    <DataAnnotationsValidator />

    <input type="hidden" name="Tarea.Id" value="@Id" />

    <div class="mb-3">
        <label for="Fecha" class="form-label">Fecha</label>
        <InputDate id="Fecha" @bind-Value="Model.Fecha" class="form-control" />
        <ValidationMessage For="() => Model.Fecha" class="text-danger" />
    </div>

    <div class="mb-3">
        <label for="HermanoId" class="form-label">Hermano</label>        
        <InputSelect class="form-select" @bind-Value="Model.HermanoId">
            <option value=''>Selecione</option>
            
            @foreach (var h in Hermanos)
            {
                <option value='@h.Id'>@h.Nombre</option>
            }

        </InputSelect>
        <ValidationMessage For="() => Model.HermanoId" class="text-danger" />
    </div>

    <div class="mb-3">
        <label for="Descripcion" class="form-label">Descripcion</label>
        <InputText id="Descripcion" @bind-Value="Model.Descripcion" class="form-control" />
        <ValidationMessage For="() => Model.Descripcion" class="text-danger" />
    </div>

    <div class="d-flex justify-content-end">

        <a href="/tareas" class="btn btn-secondary">
            <i class="bi bi-arrow-left"></i>
            Voltar
        </a>

        <button type="submit" class="btn btn-primary ms-2">
            <i class="bi bi-check-circle"></i>
            Salvar Tarea
        </button>
    </div>

</EditForm>


@code {

    public string Titulo => Id != 0 ? "Editar Tarea" : "Criar Tarea";

    [Parameter]
    public int Id { get; set; }

    [SupplyParameterFromForm]
    public Tarea Model { get; set; } = new();

    public List<Hermano> Hermanos { get; set; } = new();

    protected override async Task OnInitializedAsync()
    {
        Hermanos = await _hermanosRepository.GetAll();

        if (Id > 0)
            Model = await _tareasRepository.Get(Id);
    }

    public async Task Save()
    {
        await _tareasRepository.Upsert(Model);
        _navigation.NavigateTo("/tareas");
    }
}
